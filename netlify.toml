[build]
  publish = "."
  command = "npm install"
  functions = "netlify/functions"

# Environment variables are NOT set here.
# Add them in the Netlify UI: Site configuration -> Environment variables.
# See README.md for the full list of variable names and where to get them.
